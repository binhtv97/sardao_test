import RNConfig from 'react-native-config';

const configs = {
  API_URL: RNConfig.API_URL,
  API_KEY: RNConfig.API_KEY,
  API_TOKEN: RNConfig.API_TOKEN,
  IMAGE_URL: RNConfig.IMAGE_URL,
};

export default configs;
